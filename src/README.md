## STARTING UI 

```shell
uv run python .\src\start_ui_host.py --seats 2 --ugo ui_weights/even_net/weights.pt
```

This command uses the default project dependencies and the repository-pinned Python 3.12 runtime.

